import Foundation

// TODO: Core state machine for Mafia — owned and run by the host device only
// Handles phase transitions: lobby → night → day → voting → (repeat) → gameOver
// After each mutation, broadcast PublicSnapshot to all peers and PrivateSnapshot to each player

class MafiaGameEngine {
}
