import SwiftUI

// TODO: Role-gated night action UI — Mafia picks a kill target, Doctor picks a save target
// Dead players see a waiting screen; actions are sent to host as PlayerAction

struct NightPhaseView: View {
    var body: some View {
        // TODO: implement
        EmptyView()
    }
}
