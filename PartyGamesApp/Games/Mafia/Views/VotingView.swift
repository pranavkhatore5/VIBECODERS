import SwiftUI

// TODO: Each alive player votes to eliminate a suspect
// Tie vote = no elimination; result sent to host as PlayerAction

struct VotingView: View {
    var body: some View {
        // TODO: implement
        EmptyView()
    }
}
