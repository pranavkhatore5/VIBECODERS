import Foundation

// TODO: Define GameState — the full state of a Mafia game (players, phase, votes, night actions)
// Must be Codable so it can be serialised and broadcast over MultipeerConnectivity

struct GameState: Codable {
}
